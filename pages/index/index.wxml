<!-- index.wxml -->
<view class="title">日历</view>
<calendar today="{{true}}" timer="{{true}}" now="{{true}}" bind:select="select" bind:selectTime="selectTime" bind:today="jumpTotoday" bind:now='jumpToNow'></calendar>